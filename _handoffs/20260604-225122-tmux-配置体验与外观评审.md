# Tmux 配置体验与外观评审

## Metadata

- Generated at: `2026-06-04T22:51:22+08:00`
- Intent: `review`
- Base directory: `/Users/zish/GithubRepos/dotfiles`
- Git repo root: `/Users/zish/GithubRepos/dotfiles`
- Git branch: `main`

## Summary

当前 dotfiles 的 tmux 配置已整理成评审包。请根据实际使用体验评估快捷键、窗口/面板信息密度、状态栏可读性、浅色 GitHub Light 外观，以及哪些地方可以变得更漂亮、更容易使用。

## Status

- 主配置位于 tmux/.tmux.conf，共 321 行；包含基础行为、TPM 插件、fzf/popup 工具、GitHub Light 状态栏主题、pane border、自动窗口命名、Codex badge 和 pane layout 图标刷新。
- 关键工作流：<prefix> g 跳窗口/面板，<prefix> s 搜索工作区，<prefix> G lazygit popup，<prefix> B btop popup，<prefix> Space 临时 shell，<prefix> v/V spymux 预览，<prefix> Tab extrakto。
- 复制链路使用 copy-to-clipboard.sh，优先 pbcopy/reattach-to-user-namespace，兼容 wl-copy/xclip/xsel/clip.exe。
- 验证：bash -n 覆盖主要 shell 脚本通过；python3 -m py_compile 覆盖 Python 脚本通过；tmux 3.6b 下 tmux source-file -n tmux/.tmux.conf dry-run 通过。

## Decisions

- 本次只打包和提出评审问题，不直接修改 tmux 配置。
- handoff 聚焦体验与外观，不展开重构建议，除非影响使用体验或视觉稳定性。

## Next Steps

- 请实际使用 1-2 个工作日后回答：最常用的 5 个快捷键是什么？有没有按键冲突、记不住、或容易误触的地方？
- 请评审状态栏：当前窗口、pane layout signal、Codex running/done badge、last-active 标记、路径截断是否足够清楚；如果信息过密，请指出应隐藏或合并的字段。
- 请评审外观：GitHub Light 调色板、浅蓝选中块、单线 pane border、底部状态栏是否顺眼；是否需要 dark/light 双主题、Catppuccin/Tokyo Night/Rose Pine 变体，或更细的视觉层级。
- 请评审易用性：workspace-search 的打开目标列表是否过长，是否需要分组、默认动作、最近项目、常用 repo pinning，或把 VS Code/Zed/Windsurf/Nvim 行为拆成更直接的快捷键。
- 请检查 popup 工具尺寸和位置：90%x80% 是否合适，lazygit/btop/IPython/spymux 是否需要独立尺寸、标题、边框颜色或退出提示。

## Open Questions

- 状态栏应继续追求极简，还是接受更丰富的信息，例如 battery/weather/cpu/pomodoro 的右侧区块？
- 窗口列表现在偏 compact，只显示 index、layout signal 和 Codex badge；是否需要恢复窗口名、命令图标、dirty/worktree 状态或 session 名？
- 你更喜欢面板焦点只靠蓝色边框表达，还是需要 active pane 有轻微背景、标题、或更明显的焦点提示？

## Risks And Assumptions

- 配置里同时出现 /Users/zish/GithubRepos 和 /Users/zish/GitHubRepos；当前 macOS 路径都能列出，但跨大小写敏感文件系统可能不稳。
- workspace-search.sh 对路径参数未统一加引号；带空格或特殊字符的目录可能在 cd/new-window/open/copy path 时出错。
- 状态栏依赖 Nerd Font 图标；字体不完整时 Codex/layout/window 图标会显示为方框或乱码。
- Codex badge 和 pane layout 刷新各有 daemon，默认 2 秒轮询；当前设计已做节流，但如果窗口/面板很多，仍建议观察 tmux CPU。
- TPM 插件较多，首次安装和跨机器恢复时需要确认 ~/.tmux/plugins/tpm 存在且插件已安装。

## Notes

- tmux 版本检查结果：tmux 3.6b。
- tmux source-file -n dry-run 在沙箱外连接 tmux socket 后执行，exit code 0 且无输出。

## Context Files

### `tmux/.tmux.conf` (lines 1-87)
Resolved path: `/Users/zish/GithubRepos/dotfiles/tmux/.tmux.conf`

```text
 1 | ##### Basics #####
 2 |
 3 | # Start counting from 1
 4 | set -g base-index 1
 5 | setw -g pane-base-index 1
 6 | set -g renumber-windows on    # Renumber windows after close
 7 | set -g focus-events on        # Let Neovim receive focus events for autoread/checktime
 8 |
 9 |
10 |
11 | # Clipboard & history
12 | set-option -g set-clipboard on
13 | set-option -g @fastcopy-action 'tmux load-buffer -w -'
14 | set-option -g history-limit 100000   # Scrollback size (lines)
15 |
16 | # Mouse (selection/scroll/resize). Hold Shift to use terminal's native selection.
17 | set -g mouse on
18 |
19 | # Use vi keys in copy mode
20 | set-window-option -g mode-keys vi
21 | bind-key -T copy-mode-vi v send -X begin-selection
22 | bind-key -T copy-mode-vi V send -X select-line
23 | bind-key -T copy-mode-vi y send -X copy-pipe-and-cancel "$HOME/GithubRepos/dotfiles/tmux/copy-to-clipboard.sh"
24 | bind-key -T copy-mode-vi Enter send -X copy-pipe-and-cancel "$HOME/GithubRepos/dotfiles/tmux/copy-to-clipboard.sh"
25 | bind-key -T copy-mode Enter send -X copy-pipe-and-cancel "$HOME/GithubRepos/dotfiles/tmux/copy-to-clipboard.sh"
26 |
27 | # Toggle mouse quickly: <prefix> + m
28 | bind-key m set -g mouse \; display-message "mouse: #{?mouse,on,off}"
29 |
30 | # Reload tmux config: <prefix> + r
31 | bind-key r source-file ~/.tmux.conf \; display-message "Reloaded ~/.tmux.conf"
32 |
33 | # Refresh client: <prefix> + R
34 | bind-key R refresh-client
35 |
36 | # tmux-yank: make copy-mode yank always use our cross-platform clipboard helper
37 | if-shell '[ -x "$HOME/GithubRepos/dotfiles/tmux/copy-to-clipboard.sh" ]' \
38 |   'set -g @override_copy_command "$HOME/GithubRepos/dotfiles/tmux/copy-to-clipboard.sh"' \
39 |   'set -gu @override_copy_command'
40 |
41 | # Terminal: 256 colors + TrueColor
42 | set -g default-terminal "tmux-256color"
43 | set -g terminal-overrides ",*:RGB"
44 |
45 |
46 | ##### Plugins (TPM) #####
47 |
48 | set -g @plugin 'tmux-plugins/tpm'
49 |
50 | # CPU / weather / battery / focus
51 | set -g @plugin 'tmux-plugins/tmux-cpu'
52 | set -g @plugin 'xamut/tmux-weather'
53 | set -g @plugin 'tmux-plugins/tmux-battery'
54 | set -g @plugin 'olimorris/tmux-pomodoro-plus'
55 |
56 | # Copy enhancements
57 | # fastcopy: <prefix> + f
58 | set -g @plugin 'abhinav/tmux-fastcopy'
59 | # extrakto: <prefix> + Tab to open, Tab to insert, Enter to copy
60 | set -g @plugin 'laktak/extrakto'
61 | set -g @extrakto_key 'none'
62 | set -g @extrakto_clip_tool "$HOME/GithubRepos/dotfiles/tmux/copy-to-clipboard.sh"
63 | # yank: <prefix> + y
64 | set -g @plugin 'tmux-plugins/tmux-yank'
65 |
66 | # Session persistence
67 | # <prefix> + Ctrl-s to save, <prefix> + Ctrl-r to restore
68 | set -g @plugin 'tmux-plugins/tmux-resurrect'
69 | set -g @plugin 'tmux-plugins/tmux-continuum'
70 |
71 | # fzf integration
72 | set -g @plugin 'sainnhe/tmux-fzf'
73 |
74 | # treemux (project tree via nvim)
75 | set -g @treemux-tree 'e'
76 | set -gu @-treemux-key-Tab
77 | set -g @treemux-tree-nvim-init-file '~/.tmux/plugins/treemux/configs/treemux_init.lua'
78 | set -g @plugin 'kiyoon/treemux'
79 |
80 | # Status bar theme (with prefix indicator)
81 | set -g @plugin 'niksingh710/minimal-tmux-status'
82 |
83 | # Jump between windows
84 | # <prefix> + j fuzzy-jump by window name
85 | set -g @plugin 'schasse/tmux-jump'
86 |
87 |
```

### `tmux/.tmux.conf` (lines 88-144)
Resolved path: `/Users/zish/GithubRepos/dotfiles/tmux/.tmux.conf`

```text
 88 | ##### tmux-fzf config #####
 89 |
 90 | # Switch windows via fzf: <prefix> + g
 91 | bind-key "g" run-shell -b "~/GithubRepos/dotfiles/tmux/fzf-goto.sh"
 92 |
 93 | # Window display format / behavior (read by the plugin)
 94 | TMUX_FZF_WINDOW_FORMAT="[#{window_name}] #{window_index} #{b:pane_current_path} #{pane_current_command} [#{pane_width}x#{pane_height}] [history #{history_size}/#{history_limit}, #{history_bytes} bytes] #{?pane_active,[active],[inactive]}"
 95 | TMUX_FZF_OPTIONS="-p -w 90% -h 60% -m"
 96 | TMUX_FZF_PREVIEW=0
 97 |
 98 | # Workspace search script: <prefix> + s
 99 | bind-key "s" run-shell -b "~/GithubRepos/dotfiles/tmux/workspace-search.sh"
100 |
101 | ##### spymux #####
102 |
103 | # <prefix> + v preview all panes (tmux >= 3.2 uses popup)
104 | if -F '#{>=:#{version},3.2}' \
105 |   'bind-key "v" display-popup -E -w 90% -h 80% -d "#{pane_current_path}" "spymux"' \
106 |   'bind-key "v" new-window -n "spymux" "spymux"'
107 |
108 | # <prefix> + V preview codex panes only
109 | # Note: macOS/Linux 上 #{pane_current_command} 可能会被截断到 15 个字符（例如 codex-aarch64-a）
110 | if -F '#{>=:#{version},3.2}' \
111 |   'bind-key "V" display-popup -E -w 90% -h 80% -d "#{pane_current_path}" "spymux -c codex,codex-aarch64-a,codex-x86_64-ap,codex-aarch64-u,codex-x86_64-un"' \
112 |   'bind-key "V" new-window -n "spymux-codex" "spymux -c codex,codex-aarch64-a,codex-x86_64-ap,codex-aarch64-u,codex-x86_64-un"'
113 |
114 |
115 | ##### IPython Popup #####
116 |
117 | # <prefix> + i quick IPython (tmux >= 3.2 uses popup)
118 | if -F '#{>=:#{version},3.2}' \
119 |   'bind-key "i" display-popup -E -T "IPython" -w 90% -h 80% -d "#{pane_current_path}" "ipython"' \
120 |   'bind-key "i" new-window -n "ipython" -c "#{pane_current_path}" "ipython"'
121 |
122 |
123 | ##### Popup tools #####
124 |
125 | # <prefix> + G lazygit popup (tmux >= 3.2 uses popup)
126 | if -F '#{>=:#{version},3.2}' \
127 |   'bind-key "G" if-shell "command -v lazygit >/dev/null 2>&1" "display-popup -E -T \"lazygit\" -w 90% -h 80% -d \"#{pane_current_path}\" \"env LANG=en_US.UTF-8 lazygit\"" "display-message \"lazygit is not installed\""' \
128 |   'bind-key "G" if-shell "command -v lazygit >/dev/null 2>&1" "new-window -n \"lazygit\" -c \"#{pane_current_path}\" \"env LANG=en_US.UTF-8 lazygit\"" "display-message \"lazygit is not installed\""'
129 |
130 | # <prefix> + B btop popup (tmux >= 3.2 uses popup)
131 | if -F '#{>=:#{version},3.2}' \
132 |   'bind-key "B" if-shell "command -v btop >/dev/null 2>&1" "display-popup -E -T \"btop\" -w 90% -h 80% -d \"#{pane_current_path}\" \"btop\"" "display-message \"btop is not installed\""' \
133 |   'bind-key "B" if-shell "command -v btop >/dev/null 2>&1" "new-window -n \"btop\" -c \"#{pane_current_path}\" \"btop\"" "display-message \"btop is not installed\""'
134 |
135 | # <prefix> + Space temp shell (overrides default next-layout; tmux >= 3.2 uses popup)
136 | if -F '#{>=:#{version},3.2}' \
137 |   'bind-key "Space" display-popup -E -T "Shell" -w 90% -h 80% -d "#{pane_current_path}"' \
138 |   'bind-key "Space" new-window -n "shell" -c "#{pane_current_path}"'
139 |
140 |
141 | ##### treemux #####
142 |
143 | # treemux plugin and init file are configured above; nothing else needed here
144 |
```

### `tmux/.tmux.conf` (lines 146-240)
Resolved path: `/Users/zish/GithubRepos/dotfiles/tmux/.tmux.conf`

```text
146 | ##### GitHub Light theme #####
147 | # A lightweight palette close to GitHub “Light” (Primer):
148 | #   Background:        #FFFFFF
149 | #   Subtle background: #F6F8FA
150 | #   Border:            #D0D7DE
151 | #   Text:              #24292F
152 | #   Muted text:        #57606A
153 | #   Accent (blue):     #0969DA
154 |
155 | ###### minimal-tmux-status colors ######
156 |
157 | # NOTE: minimal-tmux-status 用 @minimal-tmux-bg/@minimal-tmux-fg 来渲染「当前窗口」的选中块。
158 | # 浅蓝底 + bold 比纯蓝字更容易定位当前 window，同时避免恢复之前厚重的深蓝 pill。
159 | set -g @minimal-tmux-fg "#0969DA,bold"   # Selected text
160 | set -g @minimal-tmux-bg "#C8E1FF"        # Selected background
161 | set -g status-interval 10                # Background updater refreshes Codex badges; keep tmux polling light
162 |
163 | set -g @minimal-tmux-justify "centre"
164 | set -g @minimal-tmux-indicator true
165 | set -g @minimal-tmux-status "bottom"
166 |
167 | # Minimal inactive windows: compact split-aware signal keeps the 30-window list scannable.
168 | # The current window's split detail is shown in status-left-extra instead.
169 | set -g @minimal-tmux-window-status-format " #I#{@pane-layout-signal}#{?window_last_flag,•,}#{@codex-badge}"
170 |
171 | # Center prefix/tmux indicator
172 | set -g @minimal-tmux-indicator-str ""
173 |
174 | # Enable left status; hide the right side to avoid showing the machine hostname
175 | set -g @minimal-tmux-right false
176 | set -g @minimal-tmux-left true
177 |
178 | # Fullscreen icon
179 | set -g @minimal-tmux-expanded-icon "󰊓 "
180 |
181 | # Keep active window compact; rounded arrows make the selected block feel taller.
182 | set -g @minimal-tmux-use-arrow false
183 | set -g @minimal-tmux-right-arrow ""
184 | set -g @minimal-tmux-left-arrow ""
185 |
186 | # Left extra info: current pane signal + current window name (truncated) + current Codex badge
187 | # Use the same accent blue / #0969DA as the rest of the theme.
188 | set -g @minimal-tmux-status-left-extra " #[none,fg=#8C959F]│#[fg=#0969DA,bold]#{@pane-layout-signal} #{=/24/...:#{?#{==:#{pane_current_path},#{HOME}},~,#{b:pane_current_path}}}#{?window_zoomed_flag, 󰊓 ,}#{@codex-badge}#[none,fg=#D0D7DE] │"
189 | set -g @minimal-tmux-status-right-extra ""
190 |
191 | # Right: hidden
192 | set -g @minimal-tmux-status-right ""
193 |
194 | # When the theme is toggled off, keep tmux status styles consistent
195 | set -g status-position bottom
196 | set -g status on
197 | set -g status-style "fg=#24292F,bg=#F6F8FA"
198 |
199 | # Window list (effective when minimal-tmux-status is disabled)
200 | # Keep backgrounds transparent; filled status cells make the row feel taller.
201 | set -g window-status-style "fg=#57606A,bg=default"                 # muted text
202 | set -g window-status-current-style "fg=#0969DA,bg=default,bold"    # active window in accent blue
203 | set -g window-status-activity-style "fg=#0969DA,bg=default"        # activity highlighted in blue
204 | set -g window-status-separator "#[fg=#D0D7DE]│"                    # Clear but quiet window boundary
205 |
206 | # Message / command-line / copy-mode colors
207 | set -g message-style "fg=#FFFFFF,bg=#0969DA"                       # blue bg for messages
208 | set -g message-command-style "fg=#24292F,bg=#EAEEF2"               # subtle gray while entering commands
209 | setw -g mode-style "bg=#0969DA,fg=#FFFFFF"                         # copy-mode selection highlight
210 | setw -g copy-mode-selection-style "bg=#FFD54F,fg=#24292F,bold"      # make selection obvious
211 | set -g clock-mode-colour "#0969DA"                                 # Large clock color (<prefix> + t)
212 |
213 | set -g status-left-length 80
214 | set -g status-right-length 0
215 |
216 | ##### Pane borders & pane numbers (GitHub Light) #####
217 |
218 | # Keep pane content untouched; focus is shown only by pane borders.
219 | set -g window-style 'bg=default'
220 | set -g window-active-style 'bg=default'
221 |
222 | # Add one quiet spacer row between pane contents and the bottom window list.
223 | set -g pane-border-status bottom
224 | set -g pane-border-format " "
225 |
226 | # Inactive pane border: subtle but still visible on a light terminal bg.
227 | set -g pane-border-style 'fg=#D0D7DE,bg=default'
228 |
229 | # Active pane border: pure GitHub blue, no filled highlight stripe.
230 | set -g pane-active-border-style 'fg=#0969DA,bg=default,bold'
231 |
232 | # If tmux >= 3.2, keep borders as light single lines.
233 | if -F '#{>=:#{version},3.2}' 'set -g pane-border-lines single'
234 |
235 | # Colors for pane numbers shown via <prefix> + q
236 | set -g display-panes-colour "#57606A"          # muted text
237 | set -g display-panes-active-colour "#0969DA"   # accent blue
238 |
239 | # show-panes duration (ms)
240 | set -g display-panes-time 10000
```

### `tmux/.tmux.conf` (lines 243-321)
Resolved path: `/Users/zish/GithubRepos/dotfiles/tmux/.tmux.conf`

```text
243 | ##### Auto rename windows / terminal title #####
244 |
245 | # Auto-rename windows with a lightweight native tmux format.
246 | # Keep shell-free: #(...) rename formats are evaluated frequently and can peg tmux CPU.
247 | set -g automatic-rename on
248 | set -g automatic-rename-format '#{?#{==:#{pane_current_path},#{HOME}}, ~, #{b:pane_current_path}}'
249 |
250 | # After manual rename, lock window name (prevent auto/OSC reset)
251 | set-hook -g after-rename-window 'set -w automatic-rename off; set -w allow-rename off'
252 |
253 | # Refresh cached Codex badges on structural/focus events; the daemon covers process/title changes.
254 | set-hook -g client-attached[90] 'run-shell -b "$HOME/GitHubRepos/dotfiles/tmux/codex-window-badges-refresh.sh --force >/dev/null 2>&1"'
255 | set-hook -g after-new-window[90] 'run-shell -b "$HOME/GitHubRepos/dotfiles/tmux/codex-window-badges-refresh.sh --force >/dev/null 2>&1"'
256 | set-hook -g after-kill-pane[90] 'run-shell -b "$HOME/GitHubRepos/dotfiles/tmux/codex-window-badges-refresh.sh --force >/dev/null 2>&1"'
257 | set-hook -g after-split-window[90] 'run-shell -b "$HOME/GitHubRepos/dotfiles/tmux/codex-window-badges-refresh.sh --force >/dev/null 2>&1"'
258 | set-hook -g after-select-window[90] 'run-shell -b "$HOME/GitHubRepos/dotfiles/tmux/codex-window-badges-refresh.sh --force >/dev/null 2>&1"'
259 | set-hook -g after-select-pane[90] 'run-shell -b "$HOME/GitHubRepos/dotfiles/tmux/codex-window-badges-refresh.sh --force >/dev/null 2>&1"'
260 | run-shell -b "exec $HOME/GitHubRepos/dotfiles/tmux/codex-window-badges-refresh.sh --daemon >/dev/null 2>&1"
261 |
262 | # Refresh cached pane layout signals and compact per-window icons.
263 | set-hook -g client-attached[91] 'run-shell -b "$HOME/GitHubRepos/dotfiles/tmux/pane-layout-icons-refresh.py --force >/dev/null 2>&1"'
264 | set-hook -g after-new-window[91] 'run-shell -b "$HOME/GitHubRepos/dotfiles/tmux/pane-layout-icons-refresh.py --force >/dev/null 2>&1"'
265 | set-hook -g after-kill-pane[91] 'run-shell -b "$HOME/GitHubRepos/dotfiles/tmux/pane-layout-icons-refresh.py --force >/dev/null 2>&1"'
266 | set-hook -g after-split-window[91] 'run-shell -b "$HOME/GitHubRepos/dotfiles/tmux/pane-layout-icons-refresh.py --force >/dev/null 2>&1"'
267 | set-hook -g after-select-window[91] 'run-shell -b "$HOME/GitHubRepos/dotfiles/tmux/pane-layout-icons-refresh.py --force >/dev/null 2>&1"'
268 | set-hook -g after-select-pane[91] 'run-shell -b "$HOME/GitHubRepos/dotfiles/tmux/pane-layout-icons-refresh.py --force >/dev/null 2>&1"'
269 | set-hook -g window-layout-changed[91] 'run-shell -b "$HOME/GitHubRepos/dotfiles/tmux/pane-layout-icons-refresh.py --force >/dev/null 2>&1"'
270 | run-shell -b "exec $HOME/GitHubRepos/dotfiles/tmux/pane-layout-icons-refresh.py --daemon >/dev/null 2>&1"
271 |
272 | # Auto-set terminal title
273 | set -g set-titles on
274 | set -g set-titles-string 'Tmux[#{session_name} #{session_windows} #{host} #{pane_current_path}]'
275 |
276 |
277 | ##### Other behavior & key bindings #####
278 |
279 | # History limit (already set above)
280 | # set-option -g history-limit 100000
281 |
282 | # Keep current directory when splitting panes
283 | bind '"' split-window -v -c "#{pane_current_path}"
284 | bind '%' split-window -h -c "#{pane_current_path}"
285 |
286 | # Fast window reordering (Vim-style): <prefix> + H/L move left/right, <prefix> + K choose target index
287 | bind -r H swap-window -t -1
288 | bind -r L swap-window -t +1
289 | bind K command-prompt -p "Move current window to index" "move-window -r -t '%%'"
290 |
291 | # New window after current (keep cwd): <prefix> + a
292 | bind-key a new-window -a -c "#{pane_current_path}"
293 |
294 | # tmux-jump: <prefix> + j search and jump by window name (plugin declared above)
295 |
296 |
297 | ##### Default shell (by OS) #####
298 |
299 | if-shell "[ $(uname) = 'Linux' ]" \
300 |     'set-option -g default-shell /usr/bin/zsh' \
301 |     'set-option -g default-shell /bin/zsh'
302 |
303 |
304 | ##### Custom window list format (lazy load) #####
305 |
306 | # Remove this hook if you don't need to reload window format on client attach
307 | # set-hook -g client-attached "run-shell 'tmux source-file ~/GithubRepos/dotfiles/tmux/window-format.conf'"
308 |
309 |
310 | ##### TPM init #####
311 |
312 | run '~/.tmux/plugins/tpm/tpm'
313 |
314 | ##### Post-TPM key overrides #####
315 |
316 | # Keep <prefix> + Tab on extrakto even if treemux/default plugin bindings were loaded earlier.
317 | unbind-key -q -T prefix Tab
318 | bind-key Tab run-shell "~/.tmux/plugins/extrakto/scripts/open.sh \"#{pane_id}\""
319 |
320 | # Hide the right status segment after the theme plugin has rendered it.
321 | set -g status-right ""
```

### `tmux/fzf-goto.sh` (lines 1-125)
Resolved path: `/Users/zish/GithubRepos/dotfiles/tmux/fzf-goto.sh`

```bash
  1 | #!/usr/bin/env bash
  2 | set -euo pipefail
  3 |
  4 | script_dir="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
  5 | preview_script="${script_dir}/fzf-goto-preview.sh"
  6 |
  7 | session="$(tmux display-message -p '#S' 2>/dev/null || true)"
  8 | if [[ -z "$session" ]]; then
  9 |   exit 0
 10 | fi
 11 |
 12 | tmux_fzf_envs="${HOME}/.tmux/plugins/tmux-fzf/scripts/.envs"
 13 | if [[ -f "$tmux_fzf_envs" ]]; then
 14 |   # shellcheck source=/dev/null
 15 |   source "$tmux_fzf_envs"
 16 | fi
 17 |
 18 | fzf_bin="${TMUX_FZF_BIN:-}"
 19 | if [[ -z "$fzf_bin" ]]; then
 20 |   if command -v fzf-tmux >/dev/null 2>&1; then
 21 |     fzf_bin="$(command -v fzf-tmux)"
 22 |   elif command -v fzf >/dev/null 2>&1; then
 23 |     fzf_bin="$(command -v fzf)"
 24 |   else
 25 |     tmux display-message "fzf not found"
 26 |     exit 0
 27 |   fi
 28 | fi
 29 |
 30 | is_fzf_tmux=0
 31 | case "$fzf_bin" in
 32 |   *fzf-tmux | */.fzf-tmux) is_fzf_tmux=1 ;;
 33 | esac
 34 |
 35 | fzf_cmd=("$fzf_bin")
 36 | if [[ -n "${TMUX_FZF_OPTIONS:-}" && $is_fzf_tmux -eq 1 ]]; then
 37 |   # shellcheck disable=SC2206
 38 |   fzf_cmd+=(${TMUX_FZF_OPTIONS})
 39 | elif [[ $is_fzf_tmux -eq 1 ]]; then
 40 |   fzf_cmd+=(-p -w 62% -h 38%)
 41 | else
 42 |   fzf_cmd+=(--height=40% --layout=reverse --border)
 43 | fi
 44 |
 45 | printf -v preview_cmd '%q %q {}' "$preview_script" "$session"
 46 |
 47 | run_fzf() {
 48 |   local header="$1"
 49 |   local expect="${2:-}"
 50 |
 51 |   local -a cmd=("${fzf_cmd[@]}")
 52 |   cmd+=(--no-multi --delimiter=$'\t' --with-nth=3.. --header="$header")
 53 |   if [[ -n "$expect" ]]; then
 54 |     cmd+=(--expect="$expect")
 55 |   fi
 56 |   preview_window="right:60%:wrap"
 57 |   if [[ "${TMUX_FZF_PREVIEW:-1}" == "0" ]]; then
 58 |     preview_window="${preview_window}:hidden"
 59 |   fi
 60 |   cmd+=(--bind "ctrl-/:toggle-preview" --preview="$preview_cmd" --preview-window="$preview_window")
 61 |
 62 |   "${cmd[@]}"
 63 | }
 64 |
 65 | goto_window() {
 66 |   local win="$1"
 67 |   tmux select-window -t "${session}:${win}" 2>/dev/null || true
 68 | }
 69 |
 70 | goto_pane() {
 71 |   local pane_id="$1" # <win>.<pane>
 72 |   local win="${pane_id%%.*}"
 73 |   goto_window "$win"
 74 |   tmux select-pane -t "${session}:${pane_id}" 2>/dev/null || true
 75 | }
 76 |
 77 | select_pane_in_window() {
 78 |   local win="$1"
 79 |   local panes
 80 |   panes="$(tmux list-panes -t "${session}:${win}" -F $'P\t#{window_index}.#{pane_index}\t[P] #{window_index}.#{pane_index} [#{pane_title}] #{pane_current_command}  #{pane_current_path} #{?pane_active,[active],[inactive]}' 2>/dev/null || true)"
 81 |   [[ -z "$panes" ]] && exit 0
 82 |
 83 |   local out key selected
 84 |   out="$(printf '%s\n' "$panes" | run_fzf "Panes in window ${win}  (Enter: switch  Esc: cancel)" "")" || exit 0
 85 |   [[ -z "$out" ]] && exit 0
 86 |
 87 |   selected="$out"
 88 |   local type id _
 89 |   IFS=$'\t' read -r type id _ <<<"$selected"
 90 |   [[ "$type" == "P" && -n "$id" ]] && goto_pane "$id"
 91 | }
 92 |
 93 | windows="$(tmux list-windows -t "$session" -F $'W\t#{window_index}\t[W] #{window_index}: #{window_name}  (panes #{window_panes})  #{pane_current_path} #{?window_active,[active],}' 2>/dev/null || true)"
 94 | [[ -z "$windows" ]] && exit 0
 95 |
 96 | out="$(printf '%s\n' "$windows" | run_fzf "Go to Window  (Enter: switch  Ctrl-p: panes  Ctrl-/: preview)" "ctrl-p")" || exit 0
 97 | [[ -z "$out" ]] && exit 0
 98 |
 99 | key=""
100 | selected=""
101 | if [[ "$out" == *$'\n'* ]]; then
102 |   key="${out%%$'\n'*}"
103 |   rest="${out#*$'\n'}"
104 |   selected="${rest%%$'\n'*}"
105 | else
106 |   selected="$out"
107 | fi
108 |
109 | [[ -z "$selected" ]] && exit 0
110 |
111 | type=""
112 | id=""
113 | IFS=$'\t' read -r type id _ <<<"$selected"
114 |
115 | if [[ "$type" == "W" ]]; then
116 |   [[ -z "$id" ]] && exit 0
117 |   if [[ "$key" == "ctrl-p" ]]; then
118 |     select_pane_in_window "$id"
119 |   else
120 |     goto_window "$id"
121 |   fi
122 |   exit 0
123 | fi
124 |
125 | exit 0
```

### `tmux/workspace-search.sh` (lines 1-67)
Resolved path: `/Users/zish/GithubRepos/dotfiles/tmux/workspace-search.sh`

```bash
 1 | #!/usr/bin/env bash
 2 | # tmux workspace search
 3 | # 2. 创建一个 fzf 窗口 用于搜索文件夹 忽略 .git node_modules .venv venv
 4 |
 5 | # Function to sanitize and handle paths with special characters like spaces
 6 | selected_file=$(fd -t d -E .git -E node_modules -E .venv -E venv -E 'Library/' -E 'Applications/' -d 5 . ~/ \
 7 | | fzf  --reverse --border --ansi --prompt="Search workspace: " \
 8 | --preview="tree -C {} | head -200" --tmux 90%,50%
 9 | )
10 |
11 | if [ -z "$selected_file" ]; then
12 |     exit 0
13 | fi
14 |
15 | # Array of actions
16 | actions=(
17 |     "current tmux window"
18 |     "new tmux window"
19 |     "new tmux pane"
20 |     "vscode"
21 |     "new tmux window & vscode"
22 |     "zed"
23 |     "windsurf"
24 |     "vim"
25 |     "new tmux window & vim"
26 |     "finder"
27 |     "copy path"
28 | )
29 | selected_option=$(printf "%s\n" "${actions[@]}" | fzf --reverse --border --ansi --prompt="Open with: " --tmux 30%,50% --preview="echo $selected_file" --preview-window=down:3:wrap)
30 |
31 | case $selected_option in
32 |     "current tmux window")
33 |         tmux send-keys -t 0 "cd $selected_file" Enter
34 |         ;;
35 |     "new tmux window")
36 |         tmux new-window -c $selected_file
37 |         ;;
38 |     "new tmux pane")
39 |         tmux split-window -c $selected_file
40 |         ;;
41 |     "vscode")
42 |         code $selected_file
43 |         ;;
44 |     "new tmux window & vscode")
45 |         tmux new-window -c $selected_file
46 |         code $selected_file
47 |         ;;
48 |     "zed")
49 |         zed $selected_file
50 |         ;;
51 |     "windsurf")
52 |         windsurf $selected_file
53 |         ;;
54 |     "vim")
55 |         tmux send-keys -t 0 "nvim $selected_file" Enter
56 |         ;;
57 |     "new tmux window & vim")
58 |         tmux new-window -c $selected_file
59 |         tmux send-keys -t 0 "nvim $selected_file" Enter
60 |         ;;
61 |     "finder")
62 |         open $selected_file
63 |         ;;
64 |     "copy path")
65 |         echo $selected_file | pbcopy
66 |         ;;
67 | esac
```

### `tmux/copy-to-clipboard.sh` (lines 1-24)
Resolved path: `/Users/zish/GithubRepos/dotfiles/tmux/copy-to-clipboard.sh`

```bash
 1 | #!/usr/bin/env bash
 2 | set -euo pipefail
 3 |
 4 | if command -v pbcopy >/dev/null 2>&1; then
 5 |   if command -v reattach-to-user-namespace >/dev/null 2>&1; then
 6 |     reattach-to-user-namespace pbcopy
 7 |   elif [[ -x /opt/homebrew/bin/reattach-to-user-namespace ]]; then
 8 |     /opt/homebrew/bin/reattach-to-user-namespace pbcopy
 9 |   elif [[ -x /usr/local/bin/reattach-to-user-namespace ]]; then
10 |     /usr/local/bin/reattach-to-user-namespace pbcopy
11 |   else
12 |     pbcopy
13 |   fi
14 | elif command -v wl-copy >/dev/null 2>&1; then
15 |   wl-copy
16 | elif command -v xclip >/dev/null 2>&1; then
17 |   xclip -in -selection clipboard
18 | elif command -v xsel >/dev/null 2>&1; then
19 |   xsel --clipboard --input
20 | elif command -v clip.exe >/dev/null 2>&1; then
21 |   clip.exe
22 | else
23 |   cat >/dev/null
24 | fi
```

### `tmux/codex-window-badges-refresh.sh` (lines 1-120)
Resolved path: `/Users/zish/GithubRepos/dotfiles/tmux/codex-window-badges-refresh.sh`

```bash
  1 | #!/usr/bin/env bash
  2 |
  3 | set -euo pipefail
  4 |
  5 | STATE_DIR="${XDG_CACHE_HOME:-$HOME/.cache}/codex-tmux-status"
  6 | REFRESH_INTERVAL="${CODEX_TMUX_BADGE_REFRESH_INTERVAL:-2}"
  7 | DAEMON_INTERVAL="${CODEX_TMUX_BADGE_DAEMON_INTERVAL:-2}"
  8 | GC_INTERVAL="${CODEX_TMUX_BADGE_GC_INTERVAL:-60}"
  9 | RUN_COLOR="#0969DA"
 10 | RUN_MUTED_COLOR="#8C959F"
 11 | DONE_COLOR="#1A7F37"
 12 | FORCE_REFRESH=0
 13 | MODE="${1:-}"
 14 |
 15 | state_path_for_pane() {
 16 |   printf '%s/pane-%s.json' "${STATE_DIR}" "${1#%}"
 17 | }
 18 |
 19 | is_codex_command() {
 20 |   case "${1:-}" in
 21 |     codex|codex-*) return 0 ;;
 22 |     *) return 1 ;;
 23 |   esac
 24 | }
 25 |
 26 | is_running_title() {
 27 |   case "${1:-}" in
 28 |     "⠋ "*|"⠙ "*|"⠹ "*|"⠸ "*|"⠼ "*|"⠴ "*|"⠦ "*|"⠧ "*|"⠇ "*|"⠏ "*) return 0 ;;
 29 |     *) return 1 ;;
 30 |   esac
 31 | }
 32 |
 33 | has_token() {
 34 |   local haystack needle
 35 |   haystack=$1
 36 |   needle=$2
 37 |
 38 |   case "${haystack}" in
 39 |     *"|${needle}|"*) return 0 ;;
 40 |     *) return 1 ;;
 41 |   esac
 42 | }
 43 |
 44 | append_unique_token() {
 45 |   local haystack needle
 46 |   haystack=$1
 47 |   needle=$2
 48 |
 49 |   if has_token "${haystack}" "${needle}"; then
 50 |     printf '%s' "${haystack}"
 51 |   else
 52 |     printf '%s%s|' "${haystack}" "${needle}"
 53 |   fi
 54 | }
 55 |
 56 | old_badge_for_window() {
 57 |   local old_badges window_id line id value
 58 |   old_badges=$1
 59 |   window_id=$2
 60 |
 61 |   while IFS= read -r line; do
 62 |     [[ -n "${line}" ]] || continue
 63 |     id=${line%%$'\t'*}
 64 |     value=${line#*$'\t'}
 65 |     if [[ "${id}" == "${window_id}" ]]; then
 66 |       printf '%s' "${value}"
 67 |       return 0
 68 |     fi
 69 |   done <<EOF
 70 | ${old_badges}
 71 | EOF
 72 | }
 73 |
 74 | should_refresh() {
 75 |   local stamp_path now last
 76 |
 77 |   mkdir -p "${STATE_DIR}"
 78 |
 79 |   if [[ "${FORCE_REFRESH}" == "1" ]]; then
 80 |     return 0
 81 |   fi
 82 |
 83 |   stamp_path="${STATE_DIR}/.refresh-stamp"
 84 |   now=$(date +%s)
 85 |   last=0
 86 |   if [[ -f "${stamp_path}" ]]; then
 87 |     IFS= read -r last < "${stamp_path}" || last=0
 88 |   fi
 89 |   [[ ${last} =~ ^[0-9]+$ ]] || last=0
 90 |   if (( now - last < REFRESH_INTERVAL )); then
 91 |     return 1
 92 |   fi
 93 |   printf '%s\n' "${now}" > "${stamp_path}"
 94 | }
 95 |
 96 | render_running() {
 97 |   local phase dot_color
 98 |
 99 |   phase=$(( ($(date +%s) / 2) % 2 ))
100 |   dot_color="${RUN_COLOR}"
101 |
102 |   if [[ ${phase} -eq 1 ]]; then
103 |     dot_color="${RUN_MUTED_COLOR}"
104 |   fi
105 |
106 |   printf ' #[push-default]#[fg=%s,bold]●#[pop-default]' "${dot_color}"
107 | }
108 |
109 | render_done() {
110 |   printf ' #[push-default]#[fg=%s,bold]󰄬#[pop-default]' "${DONE_COLOR}"
111 | }
112 |
113 | gc_stale_state_files() {
114 |   local current_panes path pane_num pane_id stamp_path now last
115 |
116 |   [[ -d "${STATE_DIR}" ]] || return 0
117 |
118 |   stamp_path="${STATE_DIR}/.gc-stamp"
119 |   now=$(date +%s)
120 |   last=0
```

### `tmux/codex-window-badges-refresh.sh` (lines 145-248)
Resolved path: `/Users/zish/GithubRepos/dotfiles/tmux/codex-window-badges-refresh.sh`

```bash
145 | main() {
146 |   local windows active_windows old_badges running_windows done_windows
147 |   local window_id window_active old_badge pane_id pane_cmd pane_title badge state_path
148 |
149 |   should_refresh || return 0
150 |   gc_stale_state_files
151 |
152 |   windows=$'\n'
153 |   active_windows="|"
154 |   old_badges=""
155 |   running_windows="|"
156 |   done_windows="|"
157 |
158 |   while IFS=$'\t' read -r window_id window_active old_badge; do
159 |     [[ -n "${window_id}" ]] || continue
160 |     windows="${windows}${window_id}"$'\n'
161 |     old_badges="${old_badges}${window_id}"$'\t'"${old_badge}"$'\n'
162 |     if [[ "${window_active}" == "1" ]]; then
163 |       active_windows="${active_windows}${window_id}|"
164 |     fi
165 |   done < <(tmux list-windows -a -F "#{window_id}"$'\t'"#{window_active}"$'\t'"#{@codex-badge}" 2>/dev/null || true)
166 |
167 |   while IFS=$'\t' read -r window_id pane_id pane_cmd pane_title; do
168 |     [[ -n "${window_id}" && -n "${pane_id}" ]] || continue
169 |     is_codex_command "${pane_cmd}" || continue
170 |
171 |     if is_running_title "${pane_title}"; then
172 |       running_windows=$(append_unique_token "${running_windows}" "${window_id}")
173 |       continue
174 |     fi
175 |
176 |     state_path=$(state_path_for_pane "${pane_id}")
177 |     if [[ -f "${state_path}" ]]; then
178 |       if has_token "${active_windows}" "${window_id}"; then
179 |         rm -f "${state_path}"
180 |       else
181 |         done_windows=$(append_unique_token "${done_windows}" "${window_id}")
182 |       fi
183 |     fi
184 |   done < <(tmux list-panes -a -F "#{window_id}"$'\t'"#{pane_id}"$'\t'"#{pane_current_command}"$'\t'"#{pane_title}" 2>/dev/null || true)
185 |
186 |   while IFS= read -r window_id; do
187 |     [[ -n "${window_id}" ]] || continue
188 |
189 |     badge=""
190 |     if has_token "${running_windows}" "${window_id}"; then
191 |       badge=$(render_running)
192 |     elif has_token "${done_windows}" "${window_id}"; then
193 |       badge=$(render_done)
194 |     fi
195 |
196 |     old_badge=$(old_badge_for_window "${old_badges}" "${window_id}")
197 |     if [[ "${old_badge}" != "${badge}" ]]; then
198 |       tmux set-window-option -q -t "${window_id}" @codex-badge "${badge}" >/dev/null 2>&1 || true
199 |     fi
200 |   done <<EOF
201 | ${windows}
202 | EOF
203 | }
204 |
205 | run_daemon() {
206 |   local lock_dir pid
207 |
208 |   mkdir -p "${STATE_DIR}"
209 |   lock_dir="${STATE_DIR}/.refresh-daemon.lock"
210 |
211 |   if ! mkdir "${lock_dir}" 2>/dev/null; then
212 |     pid=""
213 |     if [[ -f "${lock_dir}/pid" ]]; then
214 |       IFS= read -r pid < "${lock_dir}/pid" || pid=""
215 |     fi
216 |     if [[ -n "${pid}" ]] && kill -0 "${pid}" >/dev/null 2>&1; then
217 |       exit 0
218 |     fi
219 |     rm -rf "${lock_dir}"
220 |     mkdir "${lock_dir}" 2>/dev/null || exit 0
221 |   fi
222 |
223 |   printf '%s\n' "$$" > "${lock_dir}/pid"
224 |   cleanup_daemon() {
225 |     rm -rf "${lock_dir}"
226 |   }
227 |   trap cleanup_daemon EXIT
228 |   trap 'cleanup_daemon; exit 0' INT TERM
229 |
230 |   FORCE_REFRESH=1
231 |   while tmux has-session >/dev/null 2>&1; do
232 |     main || true
233 |     sleep "${DAEMON_INTERVAL}"
234 |   done
235 | }
236 |
237 | case "${MODE}" in
238 |   --daemon)
239 |     run_daemon
240 |     ;;
241 |   --force)
242 |     FORCE_REFRESH=1
243 |     main
244 |     ;;
245 |   *)
246 |     main
247 |     ;;
248 | esac
```

### `tmux/pane-layout-icons-refresh.py` (lines 17-42)
Resolved path: `/Users/zish/GithubRepos/dotfiles/tmux/pane-layout-icons-refresh.py`

```python
17 | TAB = "\t"
18 | STATE_DIR = Path(os.environ.get("XDG_CACHE_HOME", str(Path.home() / ".cache"))) / "tmux-pane-layout-status"
19 | REFRESH_INTERVAL = float(os.environ.get("TMUX_PANE_LAYOUT_REFRESH_INTERVAL", "2"))
20 | DAEMON_INTERVAL = float(os.environ.get("TMUX_PANE_LAYOUT_DAEMON_INTERVAL", "2"))
21 |
22 | SHELL_ICON = ""
23 | GRID_ICON = ""
24 | CODEX_ICON = ""
25 | SHELL_LIST_ICON = "·"
26 |
27 | SHELL_COMMANDS = {"sh", "zsh", "bash", "fish", "tmux"}
28 | PRIORITY_ICONS = [
29 |     CODEX_ICON,  # Codex
30 |     "",  # Vim / Neovim
31 |     "",  # Git
32 |     "󰣀",  # SSH / remote
33 |     "",  # Python
34 |     "",  # Node / JS / TS
35 |     "",  # Go
36 |     "",  # Rust
37 |     "",  # Docker
38 |     "☸",  # Kubernetes
39 |     "",  # Database
40 |     "",  # Monitor
41 | ]
42 |
```

### `tmux/pane-layout-icons-refresh.py` (lines 145-180)
Resolved path: `/Users/zish/GithubRepos/dotfiles/tmux/pane-layout-icons-refresh.py`

```python
145 | def render_signal(panes: list[Pane]) -> str:
146 |     if not panes:
147 |         return ""
148 |
149 |     ordered = sorted(panes, key=lambda pane: (pane.top, pane.left, pane.pane_id))
150 |     count = len(ordered)
151 |
152 |     if count == 1:
153 |         icon = ordered[0].icon
154 |         return icon
155 |
156 |     if count == 2:
157 |         left, right = sorted(ordered, key=lambda pane: (pane.left, pane.top, pane.pane_id))
158 |         top, bottom = sorted(ordered, key=lambda pane: (pane.top, pane.left, pane.pane_id))
159 |
160 |         if left.icon == right.icon:
161 |             if left.icon == SHELL_ICON:
162 |                 return f"{GRID_ICON}2"
163 |             return f"{left.icon}×2"
164 |
165 |         dx = abs(left.center_x - right.center_x)
166 |         dy = abs(top.center_y - bottom.center_y)
167 |
168 |         if dx >= dy:
169 |             return f"{left.icon}│{right.icon}"
170 |         return f"{top.icon}/{bottom.icon}"
171 |
172 |     icon = important_icon(ordered)
173 |     if count <= 4:
174 |         if icon == SHELL_ICON:
175 |             return f"{GRID_ICON}{count}"
176 |         return f"{icon}×{count}"
177 |
178 |     if icon == SHELL_ICON:
179 |         return f"{GRID_ICON}{count}"
180 |     return f"{icon}+{count}"
```

### `tmux/pane-layout-icons-refresh.py` (lines 272-367)
Resolved path: `/Users/zish/GithubRepos/dotfiles/tmux/pane-layout-icons-refresh.py`

```python
272 | def refresh(force: bool = False, print_only: bool = False) -> int:
273 |     if not print_only and not should_refresh(force):
274 |         return 0
275 |
276 |     old_values = list_old_values()
277 |     panes_by_window = list_panes()
278 |     changed = 0
279 |
280 |     for window_id, (old_signal, old_icon) in old_values.items():
281 |         panes = panes_by_window.get(window_id, [])
282 |         signal = render_signal(panes)
283 |         icon = render_window_icon(panes)
284 |         if print_only:
285 |             print(f"{window_id}{TAB}{old_signal}{TAB}{signal}{TAB}{old_icon}{TAB}{icon}")
286 |             continue
287 |
288 |         if signal != old_signal:
289 |             tmux(["set-window-option", "-q", "-t", window_id, "@pane-layout-signal", signal])
290 |             changed += 1
291 |
292 |         if icon != old_icon:
293 |             tmux(["set-window-option", "-q", "-t", window_id, "@pane-window-icon", icon])
294 |             changed += 1
295 |
296 |     if changed:
297 |         tmux(["refresh-client", "-S"])
298 |
299 |     return changed
300 |
301 |
302 | def run_daemon() -> int:
303 |     STATE_DIR.mkdir(parents=True, exist_ok=True)
304 |     lock_dir = STATE_DIR / ".refresh-daemon.lock"
305 |     script_digest = hashlib.sha256(Path(__file__).read_bytes()).hexdigest()
306 |
307 |     try:
308 |         lock_dir.mkdir()
309 |     except FileExistsError:
310 |         pid_path = lock_dir / "pid"
311 |         digest_path = lock_dir / "script-sha256"
312 |         try:
313 |             pid = int(pid_path.read_text().strip())
314 |             os.kill(pid, 0)
315 |             old_digest = digest_path.read_text().strip() if digest_path.exists() else ""
316 |             if old_digest == script_digest:
317 |                 return 0
318 |
319 |             os.kill(pid, 15)
320 |             time.sleep(0.2)
321 |         except (FileNotFoundError, ProcessLookupError, ValueError, PermissionError):
322 |             pass
323 |
324 |         shutil.rmtree(lock_dir, ignore_errors=True)
325 |         try:
326 |             lock_dir.mkdir()
327 |         except FileExistsError:
328 |             return 0
329 |
330 |     (lock_dir / "pid").write_text(f"{os.getpid()}\n")
331 |     (lock_dir / "script-sha256").write_text(f"{script_digest}\n")
332 |
333 |     try:
334 |         while True:
335 |             if subprocess.run(["tmux", "has-session"], stdout=subprocess.DEVNULL, stderr=subprocess.DEVNULL).returncode != 0:
336 |                 return 0
337 |
338 |             try:
339 |                 refresh(force=True)
340 |             except Exception:
341 |                 pass
342 |
343 |             time.sleep(DAEMON_INTERVAL)
344 |     finally:
345 |         shutil.rmtree(lock_dir, ignore_errors=True)
346 |
347 |
348 | def main(argv: list[str]) -> int:
349 |     parser = argparse.ArgumentParser(description="Refresh cached tmux pane layout icons.")
350 |     parser.add_argument("--daemon", action="store_true", help="run in the background until tmux exits")
351 |     parser.add_argument("--force", action="store_true", help="skip refresh throttling")
352 |     parser.add_argument("--print", action="store_true", dest="print_only", help="print computed signals without setting tmux options")
353 |     args = parser.parse_args(argv[1:])
354 |
355 |     if args.daemon:
356 |         return run_daemon()
357 |
358 |     if args.force or args.print_only:
359 |         refresh(force=True, print_only=args.print_only)
360 |         return 0
361 |
362 |     refresh()
363 |     return 0
364 |
365 |
366 | if __name__ == "__main__":
367 |     raise SystemExit(main(sys.argv))
```

## Command Outputs

### `git status --short --branch`
- Exit code: `0`

#### stdout

```text
## main...origin/main
?? scripts/.DS_Store
```

#### stderr

```text
(empty)
```

### `rg --files | rg '(^|/)(tmux|\.tmux|tmux\.conf|\.config/tmux)'`
- Exit code: `0`

#### stdout

```text
tmux/fzf-goto-preview.sh
tmux/codex-sidebar-bin.sh
tmux/pane-layout-icons-refresh.py
tmux/window-title.sh
tmux/window-format.conf
tmux/fzf-goto.sh
tmux/codex-window-badges-refresh.sh
tmux/workspace-search.sh
tmux/circled_index.sh
tmux/codex-notify-ghostty.py
tmux/copy-to-clipboard.sh
tmux/codex-window-badge.sh
```

#### stderr

```text
(empty)
```

### `tmux -V`
- Exit code: `0`

#### stdout

```text
tmux 3.6b
```

#### stderr

```text
(empty)
```

### `bash -n tmux/fzf-goto.sh`
- Exit code: `0`

#### stdout

```text
(empty)
```

#### stderr

```text
(empty)
```

### `bash -n tmux/workspace-search.sh`
- Exit code: `0`

#### stdout

```text
(empty)
```

#### stderr

```text
(empty)
```

### `bash -n tmux/copy-to-clipboard.sh`
- Exit code: `0`

#### stdout

```text
(empty)
```

#### stderr

```text
(empty)
```

### `bash -n tmux/codex-sidebar-bin.sh`
- Exit code: `0`

#### stdout

```text
(empty)
```

#### stderr

```text
(empty)
```

### `python3 -m py_compile tmux/pane-layout-icons-refresh.py tmux/codex-notify-ghostty.py`
- Exit code: `0`

#### stdout

```text
(empty)
```

#### stderr

```text
(empty)
```
